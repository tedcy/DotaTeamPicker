1 用户注册，查询名字对应的ID，返回给用户ID选择一个注册（注册成功对ID入库）
2 注册成功，那么利用getmatchhistory(account_id+each hero_id)+getmatchdetails这种方式获取到每个account_id数据
3 后台进程fetch全部match入库
    3.1 一张表要记录截止入库的match序列号，以及每个英雄的出场次数和获胜次数，记录每个英雄对抗每个英雄的出场次数和获胜次数，
        记录每个英雄配合每个英雄的出场次数和获胜次数
    (要记录入库的内容，一张表记录每场比赛参与的玩家，玩家以逗号连接，一张表记录每场比赛依次玩的英雄ID，输赢情况)
    3.2 在入库时，发现某场比赛涉及到已经注册的玩家ID，那么更新这个ID的数据
4 还要记录下每个ID参与的所有比赛，避免后台进程重复计算
